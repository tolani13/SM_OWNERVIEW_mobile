# Studio Maestro Nightly Handoff — 2026-03-02

## 1) Current deployed state
- main commit hash deployed: `a8f3771`
- API host: https://sm-ownerview-mobile.onrender.com
- UI host: https://studiomaestro.netlify.app
- Logged-out auth check: `GET /api/admin/studios` returns `{"error":"Missing bearer token.","code":"AUTH_MISSING"}` (PASS)

## 2) What is confirmed working (acceptance evidence)
- Dancers page loads with real data (founder session confirmed via UI).
- Finance page loads; key endpoints show 200/304 in Network.
- Run-sheet WCDE normalization fix is stable: fixture test passes (322 rows, 20 asserted entries).

## 3) What changed today (high level)
- Shared run-sheet normalizer added + route wired + fixture test added.
- Parity check: parse-pdf and Event Intel run-sheet paths return 410 for unsupported run-sheet usage (canonical path is `/run-sheet/import`).
- Auth hardening: x-user-* trust removed; tenant scoping uses auth context (`req.auth.*`); Event Intel gated + audit-logged.

## 4) Known costs/infra notes (facts only)
- Render Postgres is now paid (approx $6.30/mo) and DB connectivity was restored by using a fully-qualified DB host URL.
- `DATABASE_URL` name must be exact (no typos); DB host must be resolvable (include domain).

## 5) Remaining gaps / tomorrow priorities (ordered)
1. Phase 2 NOT complete: no Render python worker service; no `render.yaml` worker block; no async job processing loop proven.
2. Decide migration strategy risk: runtime bootstrap is current approach; document rationale and risk.
3. Implement Phase 2: `run_sheet_imports` enqueue endpoints + durable artifact storage (S3-compatible) + Python worker polling + `needs_review`/publish flow + `lock_at` enforcement using `event_timezone` (IANA).
4. Verify Clerk end-to-end login flow (legacy username/password still exists; Clerk UI flow not yet validated in product UI).

## 6) Exact commands/tests to rerun tomorrow
- `npm run check`
- `npm run test:fixture:run-sheet-normalizer`
- `npm run build:server`
